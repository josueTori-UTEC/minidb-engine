services:

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend

    container_name: minidb-backend

    ports:
      - "8000:8000"

    volumes:
      - ./backend:/app/backend
      - ./data:/app/data

    environment:
      - MINIDB_DATA_DIR=/app/data